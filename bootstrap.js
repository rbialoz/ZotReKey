function install(data, reason) {
    Zotero?.debug?.("zrk: install");
}

function uninstall(data, reason) {
    Zotero?.debug?.("zrk: uninstall");
}

function startup(data, reason) {
    Zotero?.debug?.("zrk: startup");
    // This is where you could load your main script
}

function shutdown(data, reason) {
    Zotero?.debug?.("zrk: shutdown");
}
